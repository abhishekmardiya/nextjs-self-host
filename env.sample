# ReqRes — create a project and keys at https://app.reqres.in/

REQ_RES_API_KEY=""
REQ_RES_PROJECT_ID=""

# Deploy (deploy.sh): public hostname only (no https://)
DOMAIN_NAME=""
# Git remote used for clone / pull (also set in ~/nextjs-self-host.env for first deploy if needed)
REPO_URL=""
# Optional. Defaults to ~/nextjs-self-host. If set in .env, must match the real install path on the server.
# APP_DIR=""

# Let's Encrypt / Certbot contact
LETS_ENCRYPT_EMAIL=""
